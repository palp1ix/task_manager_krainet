import 'package:flutter/material.dart';

class AppColors {
  // Both Themes
  static const error = Colors.red;
  static const success = Colors.green;
  static const progressIndicator = Colors.white;

  // Light Theme
  static const accentLight = Color(0xFFC400FF);
  static const whiteBackground = Color(0xFFF7F9FF);
  static const whiteBackgroundContainer = Color(0xFFEEF1FF);
  static const onWhiteBackground = Color(0xFF000000);

  // Dark Theme
  static const accentDark = Color(0xFFF45BFF);
  static const darkBackground = Color(0xFF1B1B1B);
  static const darkBackgroundContainer = Color(0xFF212121);
  static const onDarkBackground = Color(0xFFFFFFFF);
}
